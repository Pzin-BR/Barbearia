<%-include('../cabecalhoAdmin.ejs')%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Lista de Serviços</title>
<link href="/css/style.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Serviços</h1>

    <a href="/servico/add" class="btn add">Novo Serviço</a>

    <!-- Formulário de Busca -->
    <div style="margin: 20px 0; padding: 15px; background: #f5f5f5; border-radius: 5px;">
        <form method="get" action="/servico/lst" style="display: flex; gap: 10px; flex-wrap: wrap;">
            <input type="text" name="busca" placeholder="Buscar por nome ou descrição..." 
                   value="<%= busca %>" style="flex: 1; min-width: 200px; padding: 8px; border: 1px solid #ddd; border-radius: 4px;">
            <button type="submit" class="btn submit">Buscar</button>
            <a href="/servico/lst" class="btn" style="background: #999;">Limpar</a>
        </form>
    </div>

    <table class="aluno-table">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Descrição</th>
                <th>Preço</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
        <% if (Servico && Servico.length > 0) { %>
            <% (Servico || []).forEach(function(item){ %>
                <tr>
                    <td><%= item.nomeServico || '' %></td>
                    <td><%= item.descricaoServico || '' %></td>
                    <td>R$ <%= item.precoServico ? parseFloat(item.precoServico).toFixed(2) : '0.00' %></td>
                    <td>
                        <a class="btn edit" href="/servico/edt/<%= item._id %>">Editar</a>
                        <a class="btn delete" href="/servico/del/<%= item._id %>" onclick="return confirm('Excluir serviço?')">Excluir</a>
                    </td>
                </tr>
            <% }) %>
        <% } else { %>
            <tr>
                <td colspan="4" style="text-align: center; color: #999; padding: 20px;">Nenhum serviço encontrado</td>
            </tr>
        <% } %>
        </tbody>
    </table>
</div>
</body>
</html>
<%-include('../rodape.ejs')%>