<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Novo Serviço</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Novo Serviço</h1>
        <a href="/servico/lst" class="btn btn-outline-secondary">Ver lista</a>
    </div>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/servico/add" method="post" class="row g-3">
        <div class="col-md-6">
            <label for="nomeServico" class="form-label">Nome do Serviço</label>
            <input required type="text" class="form-control" id="nomeServico" name="nomeServico" value="">
        </div>

        <div class="col-md-6">
            <label for="duracaoServico" class="form-label">Duração</label>
            <input required type="text" class="form-control" id="duracaoServico" name="duracaoServico" value="">
        </div>

        <div class="col-12">
            <label for="descricaoServico" class="form-label">Descrição</label>
            <textarea required class="form-control" id="descricaoServico" name="descricaoServico" rows="3"></textarea>
        </div>

        <div class="col-md-4">
            <label for="precoServico" class="form-label">Preço (R$)</label>
            <input required type="number" step="0.01" min="0" class="form-control" id="precoServico" name="precoServico" value="">
        </div>

        <div class="col-12">
            <button type="submit" class="btn btn-primary">Salvar</button>
            <a href="/servico/lst" class="btn btn-secondary">Cancelar / Ir para lista</a>
        </div>
    </form>
</div>
</body>
</html>