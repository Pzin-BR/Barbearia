<%-include('../cabecalho.ejs')%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Novo Serviço</title>
<link href="/css/style.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Novo Serviço</h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/servico/add" method="post">
        <div class="form-group">
            <label for="nomeServico">Nome do Serviço</label>
            <input required type="text" id="nomeServico" name="nomeServico" value="">
        </div>

        <div class="form-group">
            <label for="duracaoServico">Duração</label>
            <input required type="text" id="duracaoServico" name="duracaoServico" value="">
        </div>

        <div class="form-group">
            <label for="descricaoServico">Descrição</label>
            <textarea required id="descricaoServico" name="descricaoServico" rows="3"></textarea>
        </div>

        <div class="form-group">
            <label for="precoServico">Preço (R$)</label>
            <input required type="number" step="0.01" min="0" id="precoServico" name="precoServico" value="">
        </div>

        <div class="form-group">
            <button type="submit" class="btn submit">Salvar</button>
            <a href="/servico/lst" class="btn cancelar">Cancelar / Ir para lista</a>
        </div>
    </form>
</div>
</body>
</html>
    <%-include('../rodape.ejs')%>