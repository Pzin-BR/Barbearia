<%-include('../cabecalhoAdmin.ejs')%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Novo Barbeiro</title>
<link href="/css/style.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Novo Barbeiro</h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/barbeiro/add" method="post">
        <div class="form-group">
            <label for="nomeBarbeiro">Nome</label>
            <input required type="text" id="nomeBarbeiro" name="nomeBarbeiro" value="<%= (old && old.nomeBarbeiro) ? old.nomeBarbeiro : '' %>">
        </div>

        <div class="form-group">
            <label for="especialidadeBarbeiro">Especialidade</label>
            <input required type="text" id="especialidadeBarbeiro" name="especialidadeBarbeiro" value="<%= (old && old.especialidadeBarbeiro) ? old.especialidadeBarbeiro : '' %>">
        </div>

        <div class="form-group">
            <label for="telefoneBarbeiro">Telefone</label>
            <input required type="tel" id="telefoneBarbeiro" name="telefoneBarbeiro" value="<%= (old && old.telefoneBarbeiro) ? old.telefoneBarbeiro : '' %>">
        </div>

        <div class="form-group">
            <label for="emailBarbeiro">Email</label>
            <input required type="email" id="emailBarbeiro" name="emailBarbeiro" value="<%= (old && old.emailBarbeiro) ? old.emailBarbeiro : '' %>">
        </div>

        <div class="form-group">
            <label for="senhaBarbeiro">Senha</label>
            <input required type="password" id="senhaBarbeiro" name="senhaBarbeiro" value="">
        </div>

        <div class="form-group">
            <button type="submit" class="btn submit">Salvar</button>
            <a href="/barbeiro/lst" class="btn cancelar">Cancelar / Ir para lista</a>
        </div>
    </form>
</div>
</body>
</html>
    <%-include('../rodape.ejs')%>