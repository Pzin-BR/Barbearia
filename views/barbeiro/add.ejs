<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Novo Barbeiro</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Novo Barbeiro</h1>
        <div>
            <!-- botão para ir à lista sem salvar -->
            <a href="/barbeiro/lst" class="btn btn-outline-secondary">Ver lista</a>
        </div>
    </div>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/barbeiro/add" method="post" class="row g-3">
        <div class="col-md-6">
            <label for="nomeBarbeiro" class="form-label">Nome</label>
            <input required type="text" class="form-control" id="nomeBarbeiro" name="nomeBarbeiro" value="<%= (old && old.nomeBarbeiro) ? old.nomeBarbeiro : '' %>">
        </div>

        <div class="col-md-6">
            <label for="especialidadeBarbeiro" class="form-label">Especialidade</label>
            <input required type="text" class="form-control" id="especialidadeBarbeiro" name="especialidadeBarbeiro" value="<%= (old && old.especialidadeBarbeiro) ? old.especialidadeBarbeiro : '' %>">
        </div>

        <div class="col-md-4">
            <label for="telefoneBarbeiro" class="form-label">Telefone</label>
            <input required type="tel" class="form-control" id="telefoneBarbeiro" name="telefoneBarbeiro" value="<%= (old && old.telefoneBarbeiro) ? old.telefoneBarbeiro : '' %>">
        </div>

        <div class="col-md-4">
            <label for="emailBarbeiro" class="form-label">Email</label>
            <input required type="email" class="form-control" id="emailBarbeiro" name="emailBarbeiro" value="<%= (old && old.emailBarbeiro) ? old.emailBarbeiro : '' %>">
        </div>

        <div class="col-md-4">
            <label for="senhaBarbeiro" class="form-label">Senha</label>
            <input required type="password" class="form-control" id="senhaBarbeiro" name="senhaBarbeiro" value="">
        </div>

        <div class="col-12">
            <button type="submit" class="btn btn-primary">Salvar</button>
            <a href="/barbeiro/lst" class="btn btn-secondary">Cancelar / Ir para lista</a>
        </div>
    </form>
</div>
</body>
</html>