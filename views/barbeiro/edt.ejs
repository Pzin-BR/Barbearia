<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Editar Barbeiro</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Editar Barbeiro</h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/barbeiro/edt/<%= Barbeiro && Barbeiro._id %>" method="post" class="row g-3">
        <div class="col-md-6">
            <label for="nomeBarbeiro" class="form-label">Nome</label>
            <input required type="text" class="form-control" id="nomeBarbeiro" name="nomeBarbeiro" value="<%= Barbeiro ? (Barbeiro.nomeBarbeiro || '') : '' %>">
        </div>

        <div class="col-md-6">
            <label for="especialidadeBarbeiro" class="form-label">Especialidade</label>
            <input required type="text" class="form-control" id="especialidadeBarbeiro" name="especialidadeBarbeiro" value="<%= Barbeiro ? (Barbeiro.especialidadeBarbeiro || '') : '' %>">
        </div>

        <div class="col-md-4">
            <label for="telefoneBarbeiro" class="form-label">Telefone</label>
            <input required type="tel" class="form-control" id="telefoneBarbeiro" name="telefoneBarbeiro" value="<%= Barbeiro ? (Barbeiro.telefoneBarbeiro || '') : '' %>">
        </div>

        <div class="col-md-4">
            <label for="emailBarbeiro" class="form-label">Email</label>
            <input required type="email" class="form-control" id="emailBarbeiro" name="emailBarbeiro" value="<%= Barbeiro ? (Barbeiro.emailBarbeiro || '') : '' %>">
        </div>

        <div class="col-md-4">
            <label for="senhaBarbeiro" class="form-label">Senha (preencha para alterar)</label>
            <input type="password" class="form-control" id="senhaBarbeiro" name="senhaBarbeiro" value="">
        </div>

        <div class="col-12">
            <button type="submit" class="btn btn-primary">Salvar</button>
            <a href="/barbeiro/lst" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>
</body>
</html>