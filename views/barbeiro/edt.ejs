<%-include('../cabecalho.ejs')%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Editar Barbeiro</title>
<link href="/css/style.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Editar Barbeiro</h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/barbeiro/edt/<%= Barbeiro && Barbeiro._id %>" method="post">
        <div class="form-group">
            <label for="nomeBarbeiro">Nome</label>
            <input required type="text" id="nomeBarbeiro" name="nomeBarbeiro" value="<%= Barbeiro ? (Barbeiro.nomeBarbeiro || '') : '' %>">
        </div>

        <div class="form-group">
            <label for="especialidadeBarbeiro">Especialidade</label>
            <input required type="text" id="especialidadeBarbeiro" name="especialidadeBarbeiro" value="<%= Barbeiro ? (Barbeiro.especialidadeBarbeiro || '') : '' %>">
        </div>

        <div class="form-group">
            <label for="telefoneBarbeiro">Telefone</label>
            <input required type="tel" id="telefoneBarbeiro" name="telefoneBarbeiro" value="<%= Barbeiro ? (Barbeiro.telefoneBarbeiro || '') : '' %>">
        </div>

        <div class="form-group">
            <label for="emailBarbeiro">Email</label>
            <input required type="email" id="emailBarbeiro" name="emailBarbeiro" value="<%= Barbeiro ? (Barbeiro.emailBarbeiro || '') : '' %>">
        </div>

        <div class="form-group">
            <label for="senhaBarbeiro">Senha (preencha para alterar)</label>
            <input type="password" id="senhaBarbeiro" name="senhaBarbeiro" value="">
        </div>

        <div class="form-group">
            <button type="submit" class="btn submit">Salvar</button>
            <a href="/barbeiro/lst" class="btn cancelar">Cancelar</a>
        </div>
    </form>
</div>
</body>
</html>
    <%-include('../rodape.ejs')%>