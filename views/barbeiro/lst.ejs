<%-include('../cabecalho.ejs')%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Lista de Barbeiros</title>
<link href="/css/style.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Barbeiros</h1>

    <a href="/barbeiro/add" class="btn add">Novo Barbeiro</a>

    <table class="aluno-table">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Especialidade</th>
                <th>Telefone</th>
                <th>Email</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
        <% (Barbeiro || []).forEach(function(item){ %>
            <tr>
                <td><%= item.nomeBarbeiro || '' %></td>
                <td><%= item.especialidadeBarbeiro || '' %></td>
                <td><%= item.telefoneBarbeiro || '' %></td>
                <td><%= item.emailBarbeiro || '' %></td>
                <td>
                    <a class="btn edit" href="/barbeiro/edt/<%= item._id %>">Editar</a>
                    <a class="btn delete" href="/barbeiro/del/<%= item._id %>" onclick="return confirm('Excluir barbeiro?')">Excluir</a>
                </td>
            </tr>
        <% }) %>
        </tbody>
    </table>
</div>
</body>
</html>
    <%-include('../rodape.ejs')%>