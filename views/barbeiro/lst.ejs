<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Lista de Barbeiros</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Barbeiros</h1>

    <a href="/barbeiro/add" class="btn btn-success mb-3">Novo Barbeiro</a>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Especialidade</th>
                <th>Telefone</th>
                <th>Email</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
        <% (Barbeiro || []).forEach(function(item){ %>
            <tr>
                <td><%= item.nomeBarbeiro || '' %></td>
                <td><%= item.especialidadeBarbeiro || '' %></td>
                <td><%= item.telefoneBarbeiro || '' %></td>
                <td><%= item.emailBarbeiro || '' %></td>
                <td>
                    <a class="btn btn-sm btn-primary" href="/barbeiro/edt/<%= item._id %>">Editar</a>
                    <a class="btn btn-sm btn-danger" href="/barbeiro/del/<%= item._id %>" onclick="return confirm('Excluir barbeiro?')">Excluir</a>
                </td>
            </tr>
        <% }) %>
        </tbody>
    </table>
</div>
</body>
</html>