<%-include('../cabecalhoAdmin.ejs')%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Lista de Barbeiros</title>
<link href="/css/style.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Barbeiros</h1>

    <a href="/barbeiro/add" class="btn add">Novo Barbeiro</a>

    <!-- Formulário de Busca -->
    <div style="margin: 20px 0; padding: 15px; background: #f5f5f5; border-radius: 5px;">
        <form method="get" action="/barbeiro/lst" style="display: flex; gap: 10px; flex-wrap: wrap;">
            <input type="text" name="busca" placeholder="Buscar por nome, email ou telefone..." 
                   value="<%= busca %>" style="flex: 1; min-width: 200px; padding: 8px; border: 1px solid #ddd; border-radius: 4px;">
            <button type="submit" class="btn submit">Buscar</button>
            <a href="/barbeiro/lst" class="btn" style="background: #999;">Limpar</a>
        </form>
    </div>

    <table class="aluno-table">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Email</th>
                <th>Telefone</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
        <% if (Barbeiro && Barbeiro.length > 0) { %>
            <% (Barbeiro || []).forEach(function(item){ %>
                <tr>
                    <td><%= item.nomeBarbeiro || '' %></td>
                    <td><%= item.emailBarbeiro || '' %></td>
                    <td><%= item.telefoneBarbeiro || '' %></td>
                    <td>
                        <a class="btn edit" href="/barbeiro/edt/<%= item._id %>">Editar</a>
                        <a class="btn delete" href="/barbeiro/del/<%= item._id %>" onclick="return confirm('Excluir barbeiro?')">Excluir</a>
                    </td>
                </tr>
            <% }) %>
        <% } else { %>
            <tr>
                <td colspan="4" style="text-align: center; color: #999; padding: 20px;">Nenhum barbeiro encontrado</td>
            </tr>
        <% } %>
        </tbody>
    </table>
</div>
</body>
</html>
<%-include('../rodape.ejs')%>