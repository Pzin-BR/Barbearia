<%-include('../cabecalho.ejs')%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>Editar Cliente</title>
<link href="/css/style.css" rel="stylesheet">
</head>
<body class="p-4">
<div class="container">
    <h1 class="mb-4">Editar Cliente</h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/cliente/edt/<%= Cliente && Cliente._id %>" method="post">
        <div class="form-group">
            <label for="nomeCliente">Nome</label>
            <input required type="text" id="nomeCliente" name="nomeCliente" value="<%= Cliente ? (Cliente.nomeCliente || '') : '' %>">
        </div>

        <div class="form-group">
            <label for="telefoneCliente">Telefone</label>
            <input required type="tel" id="telefoneCliente" name="telefoneCliente" value="<%= Cliente ? (Cliente.telefoneCliente || '') : '' %>">
        </div>

        <div class="form-group">
            <label for="emailCliente">Email</label>
            <input required type="email" id="emailCliente" name="emailCliente" value="<%= Cliente ? (Cliente.emailCliente || '') : '' %>">
        </div>

        <div class="form-group">
            <label for="senhaCliente">Senha (preencha para alterar)</label>
            <input type="password" id="senhaCliente" name="senhaCliente" value="">
        </div>

        <div class="form-group">
            <button type="submit" class="btn submit">Salvar</button>
            <a href="/cliente/lst" class="btn cancelar">Cancelar</a>
        </div>
    </form>
</div>
</body>
</html>
    <%-include('../rodape.ejs')%>